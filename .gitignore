# 运行时产物（导图数据 / 日志 / 转录切片 / 会话目录，运行时自动生成）
chatgraphic/work/

# 真实会话转录与导出（隐私数据，不上传）
.codely-cli/auto-saves/

# 工作区作用域扩展安装目录（codely extensions install --scope workspace；内含安装器的 .git，勿入库）
.codely-cli/extensions/

# 根目录测试沙箱（独立运行环境，不入库；注意锚定根路径，避免误伤 chatgraphic/test/）
/test/

# 系统杂项
.DS_Store
*.log
node_modules/
